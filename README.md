# 葡西之旅（Trip Planner）

這個 repo 存放「葡西之旅」的旅遊行程內容與展示用的 **靜態網站**（專案在 `docs/`）。

- **網站程式**：`docs/`（React + Vite）
- **行程內容 SSOT（單一真實來源）**：`docs/src/content/*.md`
- **部署**：GitHub Pages（workflow：`.github/workflows/deploy.yml`）

## 開發

```bash
cd docs
npm ci
npm run dev
```

